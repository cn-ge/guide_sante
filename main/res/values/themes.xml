<resources>

    <!-- Theme utilisé dans le manifest - Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorLaunch</item>
        <item name="colorPrimaryDark">@color/colorBlack</item>
        <item name="colorAccent">@color/colorGrey</item>
        <item name="android:textAllCaps">false</item> <!-- PAS DE MAJUSCULE automatique-->
    </style>


    <!-- Theme for the Launch View -->
    <style name="LaunchTheme" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

</resources>
